/* world.c — world data and puzzle flags (skeleton)
 * TODO: fill with room_t, item_t definitions and helper functions
 */

#include "world.h"
#include <stdio.h>

void world_init(void) {
    /* populate rooms, items, and flags */
    printf("world_init: not implemented (skeleton)\n");
}

/* example helper */
void debug_print_world(void) {
    printf("debug: world not built yet.\n");
}
