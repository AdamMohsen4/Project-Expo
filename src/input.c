/* input.c — board input abstraction (skeleton)
 * TODO: implement reading of buttons/switches for DTEK-V board.
 */

#include "input.h"
#include <stdio.h>

void input_init(void) {
    /* prepare input (placeholder) */
}

int input_get_action(void) {
    /* stub: return -1 = no action */
    return -1;
}
