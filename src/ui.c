/* ui.c — printing and paging helpers (skeleton) */

#include "ui.h"
#include <stdio.h>

void ui_print_paged(const char *text) {
    /* very small placeholder that prints the whole text */
    printf("%s\n", text);
}
